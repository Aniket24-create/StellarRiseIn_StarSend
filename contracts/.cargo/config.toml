# Only apply wasm target when explicitly building for wasm.
# Do NOT set [build] target here — that would break `cargo test`
# which needs to compile for the native host target.

[target.wasm32-unknown-unknown]
rustflags = ["-C", "target-feature=+sign-ext"]
